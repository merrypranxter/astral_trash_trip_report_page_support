# Trip Reports threshold

Standalone browser entry point for the Trip Reports archive.

- `index.html` provides the semantic page shell.
- `styles.css` owns page layout, material, and accessibility states.
- `trip-reports.js` owns the decorative canvas systems and interactions.

Serve the repository root and open `/web/pages/trip-reports/`. Shared fonts
live in `web/assets/fonts/`.
